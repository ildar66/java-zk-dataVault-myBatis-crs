package ru.masterdm.crs.domain.entity.meta;

/**
 * Common DB columns.
 * @author Sergey Valiev
 */
public enum CommonColumn {
    /** ID table column. */
    ID,
    /** H_ID table column. */
    H_ID,
    /** LINK_ID satellite table column. */
    LINK_ID,
    /** KEY table column. */
    KEY,
    /** LDTS table column. */
    LDTS,
    /** REMOVED table column. */
    REMOVED,
    /** DIGEST table column. */
    DIGEST,
    /** HUB_LDTS column (hub-table.ldts). */
    HUB_LDTS
}
