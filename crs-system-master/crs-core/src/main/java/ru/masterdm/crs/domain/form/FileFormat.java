package ru.masterdm.crs.domain.form;

/**
 * Form template types.
 * @author Eugene Melnikov
 */
public enum FileFormat {
    /** Excel. */
    EXCEL,
    /** DBF. */
    DBF,
    /** XML. */
    XML
}
